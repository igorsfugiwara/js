//@ts-check

//@ts
const frase = 'frontend';
const total = 100.05;
const empresas = ['Apple', 'Microsoft'];
const body = document.body; //objeto HTML
const button = document.querySelector('button');
const operacao = 100 + true;


frase.toLowerCase();

total.toFixed(); //arredonda o valor, RETORNA STRING

empresas.map(empresa => empresa.toLowerCase());

body.style.background = '#000';

button.click();


console.log(filtrarLivros(produtos));